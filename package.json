{
  "name": "express-login",
  "version": "1.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:all": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "install:all": "concurrently \"cd frontend && npm i\" \"cd backend && npm i\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
